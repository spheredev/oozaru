document.write(`<h1><b>${system.name} ${system.version} (Sphere v${system.apiVersion} L${system.apiLevel})</b></h1>`);

Dispatch.onUpdate(update);
Dispatch.onRender(render);

function update()
{
}

function render()
{
}
